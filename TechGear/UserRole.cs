namespace TechGear
{
    // Hier legen wir die verschiedenen "Rollen" (Rechte) in unserem System fest
    // Ein "enum" ist wie eine feste Liste von Begriffen, aus der wir später auswählen können
    internal enum UserRole
    {
        User,
        Admin,
        SuperAdmin
    }
}
